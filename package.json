{
  "name": "changeset-test",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/harrysolovay/changeset-test",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/plugins/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.4.0"
  },
  "scripts": {
    "release": "changeset publish && git push --folow-tags"
  }
}
